piVersion: v1
kind: Service
metadata:
        name: psweb
spec:
    selector:
            env: develop
            projet: devops-training
    type: NodePort
    ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31001
